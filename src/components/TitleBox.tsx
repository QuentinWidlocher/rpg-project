export default function TitleBox(props: { title: string }) {
  return <section class="shadow-lg shadow-primary/20 text-xl bg-primary p-5 mt-0 -m-2 rounded-xl text-primary-content">{props.title}</section>
}
